# Унифицированный шаблон решения задач N9 (Электронные таблицы)

Общая схема: во всех задачах даётся файл, в каждой строке **N натуральных чисел** (N = 4, 5, 6 или 7). Нужно отобрать строки по **двум условиям** (в 914 одно условие) и выдать **ответ** одного из типов: количество строк / наибольший номер строки / наименьший номер строки / сумма чисел в строке с наибольшим номером.

---

## Справочная таблица задач 901-917

| №   | Чисел в строке | Тип ответа | Кратко условия |
|-----|----------------|------------|----------------|
| 901 | 7              | наибольший номер строки | одно число ×3, остальные 4 различны; ср. повторяющихся > ср. всех |
| 902 | 4              | количество строк | макс < сумма трёх других; можно разбить на 2 пары с равными суммами |
| 903 | 7              | количество строк | одно ×3, одно ×2, два по 1; макс повторяющихся > макс неповторяющихся |
| 904 | 5              | наибольший номер строки | все различны; 2(min+max)=3(сумма трёх оставшихся) |
| 905 | 7              | наименьший номер строки | два ×2, три по 1; ср. повторяющихся < макс неповторяющихся |
| 906 | 6              | наибольший номер строки | одно ×3, три по 1; повторяющееся > ср. неповторяющихся |
| 907 | 7              | сумма чисел в строке | одно ×3, четыре по 1; ср. неповторяющихся ≤ повторяющееся (наиб. номер) |
| 908 | 5              | количество строк | все различны; сумма двух наибольших ≤ сумма трёх оставшихся |
| 909 | 7              | количество строк | два ×2, три по 1; ср. неповторяющихся ≤ ср. всех |
| 910 | 6              | количество строк | одно ×3, три по 1; 3·(повтор.)² > сумма квадратов неповторяющихся |
| 911 | 4              | количество строк | макс < сумма трёх других; все четыре различны |
| 912 | 4              | количество строк | макс < сумма трёх других; нельзя разбить на 2 пары с равными суммами |
| 913 | 4              | количество строк | макс < сумма трёх других; можно разбить на 2 пары с равными суммами |
| 914 | 5              | количество строк | (min+max)² > сумма квадратов трёх оставшихся |
| 915 | 5              | количество строк | все различны; 3(min+max) ≥ 2(сумма трёх оставшихся) |
| 916 | 7              | количество строк | два ×2, три по 1; ср. повторяющихся < ср. всех |
| 917 | 6              | количество строк | одно ×3, три по 1; (сумма повторяющихся)² > (сумма неповторяющихся)² |

---

## 1. Шаблон: таблица Excel

Для любой задачи подставляем свой диапазон (A1:G1 для 7 чисел, A1:E1 для 5, A1:D1 для 4 и т.д.) и свои формулы проверок.

| Ячейка | Формула | Проверка | Как работает формула |
|--------|---------|----------|------------------------|
| **A1:[X]1** | исходные N чисел строки |   | В ячейках лежат N чисел обрабатываемой строки таблицы. |
| **H1** (и далее) | формула условия 1 (равно ИСТИНА/ложь или значение) | что проверяем | Пояснение: что считает функция, что значит результат. |
| **I1** | формула условия 2 (или вспомогательная) | что проверяем | Пояснение. |
| **J1** | итоговая проверка (если нужно сравнение двух величин) | итог | Пояснение. |

**Ответ:** [тип ответа для задачи: количество строк / наибольший номер строки / наименьший номер строки / сумма чисел в строке с наибольшим номером], при которых все проверки = ИСТИНА.

---

## 2. Шаблон: псевдокод

```
вход: файл, в каждой строке N натуральных чисел (разделитель пробел)
выход: [количество строк | наибольший номер строки | наименьший номер строки | сумма чисел в строке с наибольшим номером]

[переменная ответ := 0 или не определён]

для каждой строки файла, начиная с номера n = 1:
    прочитать строку и разбить по пробелам
    a := список из N целых чисел

    если длина(a) ≠ N:
        перейти к следующей строке

    [проверка условия 1: структура чисел и/или соотношения]
    [проверка условия 2]
    если оба условия выполнены:
        [в зависимости от типа ответа:
         количество: ответ := ответ + 1
         наибольший номер: ответ := n
         наименьший номер: ответ := n; остановить цикл
         сумма в строке с наиб. номером: ответ := n; запомнить сумму(a); затем в цикле обновлять при совпадении и n больше текущего]

вывести ответ [или сумму чисел в строке ответ]
```

---

## 3. Шаблон: Python

```python
"""
[Текст задачи из условия, одна строка про файл и оба условия.]
В ответе запишите только число.
"""
# тип ответа: count | max_row | min_row | sum_in_max_row
answer = 0   # или None для min_row
n = 0
for line in open('[номер].txt'):   # или open('[номер]')
    n += 1
    a = [int(x) for x in line.split()]
    if len(a) != N:
        continue
    # проверка условия 1
    # проверка условия 2
    if condition1 and condition2:
        # count: answer += 1
        # max_row: answer = n
        # min_row: answer = n; break
        # sum_in_max_row: answer = n; row_sum = sum(a) и обновлять при бóльшем n
print(answer)
```

---

# Пример: задача 905 в трёх форматах

**Условие:** файл с 7 числами в строке; найти **наименьший** номер строки, где: два числа повторяются дважды, три по одному разу; среднее повторяющихся меньше максимума неповторяющихся.

---

## 1. Таблица Excel (905)

| Ячейка | Формула | Проверка | Как работает формула |
|--------|---------|----------|------------------------|
| A1:G1 | исходные 7 чисел строки |   | В ячейках лежат семь чисел обрабатываемой строки таблицы. |
| H1 | =СЧЁТ(УНИК(A1:G1;1))=5 | уникальных значений ровно 5 | УНИК(A1:G1;1) возвращает список уникальных значений по строкам из диапазона A1:G1. СЧЁТ считает их количество. Сравнение с 5 проверяет, что в строке ровно 5 разных чисел (два повторяются по 2 раза, три по 1 разу). |
| I1 | =СЧЁТ(УНИК(A1:G1;1;1))=3 | ровно 3 значения встречаются один раз | УНИК(A1:G1;1;1) с третьим аргументом 1 возвращает только те значения, которые встречаются ровно один раз. СЧЁТ считает их количество. Равенство 3 означает, что ровно три числа в строке неповторяющиеся. |
| J1 | =СРЗНАЧ(ЕСЛИ(СЧЁТЕСЛИ(A1:G1;A1:G1)>1;A1:G1)) | среднее повторяющихся чисел | СЧЁТЕСЛИ(A1:G1;A1:G1) для каждой ячейки считает, сколько раз её значение встречается в A1:G1. ЕСЛИ(…>1;A1:G1) оставляет только значения с количеством >1. СРЗНАЧ считает среднее, по сути (a+b)/2 для двух повторяющихся. |
| K1 | =МАКС(ЕСЛИ(СЧЁТЕСЛИ(A1:G1;A1:G1)=1;A1:G1)) | максимум неповторяющихся чисел | ЕСЛИ(СЧЁТЕСЛИ(...)=1;A1:G1) оставляет только числа, встречающиеся ровно один раз. МАКС находит среди них максимальное. |
| L1 | =J1<K1 | среднее повторяющихся меньше максимума неповторяющихся | Сравнение J1 (среднее двух повторяющихся) и K1 (максимум трёх неповторяющихся). ИСТИНА, если условие задачи выполнено. |

**Ответ:** наименьший номер строки, в которой H1 = ИСТИНА, I1 = ИСТИНА и L1 = ИСТИНА.

---

## 2. Псевдокод (905)

```
вход: файл, в каждой строке 7 натуральных чисел (разделитель пробел)
выход: наименьший номер строки, удовлетворяющей обоим условиям

ответ := не определён

для каждой строки файла, начиная с номера n = 1:
    прочитать строку и разбить по пробелам
    a := список из 7 целых чисел

    если длина(a) ≠ 7:
        перейти к следующей строке

    уникальные := множество(a)
    если размер(уникальные) ≠ 5:
        перейти к следующей строке

    повторяющиеся_2 := список значений x из уникальные, где количество вхождений x в a равно 2
    одиночные := список значений x из уникальные, где количество вхождений x в a равно 1
    если длина(повторяющиеся_2) ≠ 2 или длина(одиночные) ≠ 3:
        перейти к следующей строке

    J1 := (повторяющиеся_2[1] + повторяющиеся_2[2]) / 2
    K1 := максимум(одиночные)

    если J1 < K1:
        ответ := n
        остановить цикл

вывести ответ
```

---

## 3. Python (905)

```python
"""
Откройте файл электронной таблицы, содержащей в каждой строке семь натуральных чисел.
Определите наименьший номер строки таблицы, для чисел которой выполнены оба условия:
    1. в строке есть два числа, каждое из которых повторяется дважды, остальные три числа различны;
    2. среднее арифметическое всех повторяющихся чисел строки меньше её максимального неповторяющегося числа.
В ответе запишите только число.
"""
min_row = None
n = 0
for i in open('905'):
    n = n + 1
    a = [int(x) for x in i.split()]
    if len(a) != 7:
        continue
    rep2 = [x for x in set(a) if a.count(x) == 2]
    rep1 = [x for x in set(a) if a.count(x) == 1]
    if len(rep2) != 2 or len(rep1) != 3:
        continue
    if (rep2[0] + rep2[1]) / 2 < max(rep1):
        min_row = n
        break
print(min_row)
```

Ответ для 905: наименьший номер строки (значение получится после запуска по файлу данных `905`).
