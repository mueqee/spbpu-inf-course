```python
def f(x, y): # x - текующее число, y - куда нужно придти
	if x > y: return 0
	if x == y: return 1
	if x < y:
		return f(x + 1, y) + f(x + 2, y) + f(x * 3, y)
		
print(f(1, 3))
```

```python
# На возрастание
def f(x, y): # x - текующее число, y - куда нужно придти
	if x > y or x == 12: return 0 # По условию не проходит через 12
	if x == y: return 1
	if x < y:
		return f(x + 1, y) + f(x + 2, y) + f(x * 3, y)
print(f(2, 9) * f(9, 19)) # По условию проходит через 9
```

```python
# На уменьшение, необходимо поменять знаки
def f(x, y):
	if x < y or x == 7: return 0
	if x == y: return 1
	if x > y:
		return f(x - 1, y) + f(x - 4, y) + f(x//3, y)

print(f(19, 13) * f(13, 2)) # По условию проходит через 9
```

```python
from functools import lru_cache

@lru_cache(None) # В упор с функцией, никаких пробелов после
def F(n):
	if n < 10: return n
	if n >= 10:
		return 3 * n + F(n-3)
  
for n in range(1, 7000):
	F(n)

print((F(6250) + 2 * F(6244))/F(6238))
```

```python
from functools import lru_cache

@lru_cache(None)
def F(n):
	return 2 * (G(n -3) + 8)

@lru_cache(None)
def G(n):
	if n < 10:
		return 2 * n
	if n >= 10:
		return G(n - 2) + 1

for n in range(1, 16000):
	F(n), G(n)

print(F(15548))
```

```python
def f(curr, end): # curr - начальное знач, end - конечное
	if curr > end:
		return 0
	if curr == end:
		return 1
	if curr < end:
	# Пишем команды исполнителя из условия
		return f(curr+1, end)+f(curr+3, end)+f(curr*2, end)

print(f(1, 15))
```

```python
def f(curr, end):
	if curr < end:
		return 0
	if curr == end:
		return 1
	if curr > end:
		return f(curr-1, end) + f(curr-3, end) + f(curr//3, end)

print(f(22, 2))
```

```python
def f(curr, end):
	if curr > end:
		return 0
	if curr == end:
		return 1
	if curr < end:
		return f(curr+1, end) + f(curr*2, end)

print(f(1, 10) * f(10, 20))
```

Там где просят работать с двоичными числами лучше подстроить под десятичную систему счисления:
- Добавить справа ноль - умножить на два
- Добавить справа единицу - умножить на два и прибавить единицу

```python
def f(curr, end):
	if curr>end:
		return 0
	if curr==end:
		return 1
	if curr<end and curr%10==9: # Сверяем последнюю цифру
		return f(curr+1, end) + f(curr+10, end)
	if curr<end and curr%10!=9:
		return f(curr+1, end) + f(curr+11, end)

print(f(25, 51))
```

```python
def f(curr, end):
	if curr > end:
		return 0
	if curr==end:
		return 1
	if curr < end:
		return f(curr+2, end) + f(curr+4, end) + f(curr + 5, end)
  
for n in range(31, 10000):
	if f(31, n) == 1001:
		print(n)
```

```python
def f(curr, end, step):
	if curr > end:
		return 0
	if curr == end and step==7:
		return 1
	if curr == end and step!=7:
		return 0
	if curr < end:
		return f(curr+1, end, step+1) + f(curr+4, end, step+1) + f(curr*2, end, step+1)

print(f(3, 27,0))
```

```python
d = set()

def f(curr, step):
	if step==8:
		if 1000<=curr<=1024:
			d.add(curr)
	else:
		f(curr+1, step+1)
		f(curr+5, step+1)
		f(curr*3, step+1)

f(1,0)

print(len(d))
```

```python
def f(curr, end, p):
	if curr > end:
		return 0
	if curr == end:
		return 1
	if curr < end and p=='*2':
		return f(curr+1, end, '+1') + f(curr+2, end, '+2')
	if curr < end and p != '*2':
		return f(curr+1, end, '+1') + f(curr+2, end, '+2') + f(curr*2, end, '*2')

print(f(1, 15, ''))
```

```python
def f(curr, end, p):
	if curr > end:
		return 0
	if curr == end:
		return p==1
	if curr < end:
		return f(curr+1, end, p) + f(curr+2, end, p) + f(curr*2, end, p+1)

print(f(2, 12, 0))
```

```python
def f(curr, end, p):
	if curr > end:
		return 0
	if curr == end:
		return p<=3
	if curr < end:
		return f(curr+2, end, p) + f(curr*3, end, p+1) + f(curr*5, end, p+1)

print(f(2, 200, 0))
```

```python
from functools import *

@lru_cache(None)
def f(curr, end, k):
	if curr%2!=0:
		k+=1
	if curr > end:
		return 0
	if curr==end:
		return k==1
	if curr<end:
		return f(curr+1, end, k) + f(curr+2, end, k) + f(curr*2, end, k)

print(f(2, 40, 0))
```

```python
def f(curr, end, k):
	if curr>end:
		return 0
	if curr%2==0:
		k+=1
	if curr==end:
		return k==6
	if curr < end:
		return f(curr+1, end, k) + f(curr+3, end, k) + f(curr+5, end, k)

print(f(3, 25, 0))
```

```python
def f(x, y):
	if x > y:
		return 0
	if x == y:
		return 1
	if x < y:
		return f(x * 2, y) + f(pow(x, 2), y) + f(pow(x, 3), y)

# нам подойдут все траектории кроме тех,
# которые содержат одновременно 4 и 16 - найдем и вычтем их
ans1 = f(2, 131072)
ans2 = f(2, 4) * f(4, 16) * f(16, 131072)
print(ans1 - ans2)
```

```python
def f(a, b, not_number):
	if a == b:
		return 1
	elif a > b or a == not_number:
		return 0
	else:
		return (f(a + 1, b, not_number) + f(a + 2, b, not_number) +
	f(a + 4, b, not_number) + f(a + 8, b, not_number))

# траектория содежит 24, но при этом не содержит 32
ans1 = f(16, 24, 32) * f(24, 48, 32)
# траектория содежит 32, но при этом не содержит 24
ans2 = f(16, 32, 24) * f(32, 48, 24)
print(ans1 + ans2)
```

