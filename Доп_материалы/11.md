# Задание 11 - Вычисление количества информации

Задание 11 делится на два типа:
- Пароли
- Пароль с дополнительными сведениями

```python
N = pow(2, i)
```
## Пароли

Пример задачи:

```
При регистрации в компьютерной системе каждому пользователю выдаётся пароль, состоящий из 11 символов и содержащий только символы А, Б, В, Г, Д, Е. Каждый такой пароль в компьютерной программе записывается минимально возможным и одинаковым целым количеством байт, при этом используют посимвольное кодирование и все символы кодируются одинаковым и минимально возможным количеством бит. Определите, сколько байт необходимо для хранения 20 паролей.
```

```python

```

```python
N = 15 # По условию пароль состоит из 15 символов
Z = 9
i = 4 # Так как pow(2, 4) = 16, чего хватит для 9 значений, нежели 3 бит, где pow(2, 3) = 8

V = N * i # Размер пароля в битах

# По условию объем нужно указать в байтах
print(ceil(V/8)) # Округляем в большую сторону, ибо нельзя терять информацию. Нельзя отбрасывать полбайта
```

```python
from math import *

a = 9
sym = 15
char = ceil(log2(a)) # Так мы сможем найти i, ceil округление в большую сторону
pswd = ceil(sym*char/8)
V = pswd * 25
```

```python
from math import ceil, log2

a = 26 + 26 + 10 # Кодировка
b = 10 # Длина пароля
i = ceil(log2(a)) # Бит на символ
c = ceil(b * i/8) # Размер пароля

user = 870/30 # Сколько всего отведено на одного пользователя. По условию 870 на 30 пользователей

print(user-c) # 29 - 8. 21 байт это доп. сведения на одного пользователя, что необходимо по условию
```

```python
from math import *

c = set()

for n in range(1,1000):
	a = 10 + 52 + 1988
	i = ceil(log2(a))
	b = ceil(n * i/8)
	if b * 1974 <= 579 * pow(2, 10):
		c.add(n)

print(max(c))
```

```python
from math import *

for k in range(1,1000):
	a = 1231
	i = ceil(log2(k))
	b = ceil(a * i/8)
	if 523872*b > 432 * pow(2, 20):
		print(k)
		break
```

```python
from math import *

a = 745
b = 10 + 999
i = ceil(log2(b))
c = ceil(a * i/8)

s = set()

for n in range(1, 100000):
	c = ceil(a * i/8) + n
	if 312 * c <= 311 * pow(2, 10):
		print(n * 312)
```

