```python
from fnmatch import fnmatch

for x in range(1917, pow(10, 10) + 1, 1917): # Добавляем единицу, ибо нужно включить последнее число
	if fnmatch(str(x), '3?12?14*5'):
		print(x, x // 1917)
```

Числа огромные, поэтому проще начать подсчет с 1917 с шагом 1917. По условию нам нужны числа, которые нацело делятся на 1917.

```python
from fnmatch import fnmatch

for x in range(84318, pow(10, 12), 84318):
	if fnmatch(str(x), '5*7?'):
		if len(str(x)) == len(set(str(x))): # По условию только уникальные
			print(x, x//84318)
```

### Функция для нахождения делителей числа

```python
def f(x):
	d = []
	for a in range(1, x+1):
		if x%a == 0:
			d.append(a)
	return d
```

### Мгновенное нахождение делителей огромных чисел

```python
def f(x):
	d = set()
	for a in range(1, int(pow(x, 0.5)) + 1):
		if x%a == 0:
			d.add(a)
			d.add(x//a)
	return sorted(d)
```


```python
def f(x):
	d = []
	for a in range(2, int(pow(x, 0.5)) + 1):
		if x%a == 0:
			d.append(a)
			d.append(x//a)
	if len(d) == 0:
		return 0
	return min(d) + max(d)

for x in range(80_001, 1000000):
	c = f(x)
	if str(c)[-1] == '4':
		print(x, c)
```

```python
from fnmatch import fnmatch

def f(x):
	d = set()
	for a in range(1, int(pow(x, 0.5)) + 1):
		if x % a == 0:
			d.add(a)
			d.add(x//a)
	return sorted(d)

for x in range(65_001, 1_000_000):
	if fnmatch(str(x), '6*97*5?'):
		if len([d for d in f(x) if d%2==0]) >= 4:
			print(x, sum([d for d in f(x) if d%2==0]))
```

```python
from fnmatch import fnmatch

def f(x):
	d = set()
	for a in range(1, int(pow(x, 0.5)) + 1):
		if x % a == 0:
			d.add(a)
			d.add(x//a)
	return sorted(d)

for x in range(pow(10, 7)):
	if fnmatch(str(x), '9?*55*7'):
		print(x, sum(f(x))%21)
```

```python
from fnmatch import fnmatch

def f(x):
	d = set()
	for a in range(1, int(pow(x, 0.5)) + 1):
		if x%a == 0:
			d.add(a)
			d.add(x//a)
	return d

for x in range(1000000):
	if fnmatch(str(x), '?6*6*?6'):
		if x % 6 == 0 and x % 7 == 0 and x % 8 == 0:
			print(x, sum(f(x)))
```

```python
from fnmatch import fnmatch

def f(x):
	d = set()
	for a in range(1, int(pow(x, 0.5)) + 1):
		if x % a == 0:
			d.add(a)
			d.add(x//a)
	return d

for x in range(1, pow(10, 7)):
	if fnmatch(str(x), '14?4*'):
		if x % 217 == 0:
			print(x, sum([d for d in f(x) if d%2!=0]))
```

```python
from fnmatch import fnmatch

c = 0

for x in range(700000, 1000000):
	if not(fnmatch(str(x), '*0??3*')):
		if not(fnmatch(str(x), '*4??2')):
			if not(fnmatch(str(x), '*1*')):
				if x % 13 == 0:
					print(x, sum([int(d) for d in str(x)]))
					c += 1
					if c == 5:
						break
```


```python
for x in range(800_000, 810_000):
	a = []
	for i in range(1, int(x / 2) + 1):
		if x % i == 0:
			if i != x and i != 9 and str(i)[-1] == '9':
				print(x, i)
```

